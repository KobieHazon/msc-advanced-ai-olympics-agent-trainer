# Curated Training Artifact

`ppo-vs-random-episode-9900/` contains the latest actor and critic pair from the recovered June 21, 2023 PPO-versus-random training run.

The original snapshot also contained hundreds of earlier checkpoints and TensorBoard event files totaling roughly 850 MB. Those generated intermediates are intentionally omitted. The final competition-facing models remain under `agents/`.
